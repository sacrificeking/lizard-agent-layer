{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lizard-agent-layer.local/schemas/loop.schema.json",
  "title": "lizard-agent-layer loop pattern",
  "type": "object",
  "$defs": {
    "safeRelativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?![A-Za-z]:)(?![\\\\/])(?!.*(?:^|[\\\\/])\\.\\.(?:[\\\\/]|$)).+$"
    }
  },
  "required": ["name", "description", "readinessLevel", "riskLevel", "cadence", "stateFile", "budgetFile", "runLogFile", "constraintsFile", "skills", "allowedActions", "deniedActions", "humanGates"],
  "properties": {
    "name": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,62}$" },
    "description": { "type": "string", "minLength": 10 },
    "readinessLevel": { "enum": ["L0", "L1", "L2", "L3"] },
    "riskLevel": { "enum": ["low", "medium", "high"] },
    "cadence": {
      "type": "object",
      "required": ["recommended", "minimum", "notes"],
      "properties": {
        "recommended": { "type": "string" },
        "minimum": { "type": "string" },
        "notes": { "type": "string" }
      },
      "additionalProperties": false
    },
    "stateFile": { "$ref": "#/$defs/safeRelativePath" },
    "budgetFile": { "$ref": "#/$defs/safeRelativePath" },
    "runLogFile": { "$ref": "#/$defs/safeRelativePath" },
    "constraintsFile": { "$ref": "#/$defs/safeRelativePath" },
    "worktreePolicyFile": { "$ref": "#/$defs/safeRelativePath" },
    "assistedPlanFile": { "$ref": "#/$defs/safeRelativePath" },
    "verifierFile": { "$ref": "#/$defs/safeRelativePath" },
    "skills": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
    "allowedActions": { "type": "array", "items": { "type": "string" } },
    "deniedActions": { "type": "array", "items": { "type": "string" } },
    "humanGates": { "type": "array", "items": { "type": "string" } },
    "modelStrategy": {
      "type": "object",
      "properties": {
        "cheap": { "type": "array", "items": { "type": "string" } },
        "strong": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": false
    },
    "successMetrics": { "type": "array", "items": { "type": "string" } },
    "notes": { "type": "string" }
  },
  "additionalProperties": false
}
