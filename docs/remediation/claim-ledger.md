# Remediation Claim Ledger

Status vocabulary: `OPEN_CONFIRMED`, `OPEN_CHANGED`, `ALREADY_FIXED_UNVERIFIED`, `CLOSED_WITH_EVIDENCE`, `SUPERSEDED`, and `NOT_REPRODUCIBLE`.

The baseline is commit `1797097d8a1c5ab5444ac82e83786ac1ccc841f6`. A finding is closed only when its complete measurable exit condition has executable evidence on every claimed host.

| ID | Current status | Current evidence or change | Evidence still required for closure |
| --- | --- | --- | --- |
| H-01 | `OPEN_CHANGED` | WP-03 requires canonical immutable install/update plans, independently supplied SHA-256 values, explicit human approval, actual Git-HEAD and executed-source binding, rederived target action/ownership/intended hashes, nested update/install binding, pre-lock checks, post-lock/pre-mutation revalidation, and applied-plan receipts; local Windows PowerShell 5.1 evidence passes 18/18 focused suites, smoke, 18/18 profile/harness combinations, and all nine full-CI gates (`.tmp/ci/ci-report-20260802091602.json`) | Independent PowerShell 7 and Unix verification of the full source/overlay/inventory/runtime/target/CLI drift matrix; authenticated approver identity remains H-09 and physical race resistance remains H-03 |
| H-02 | `OPEN_CHANGED` | WP-02 adds strict v2 validation, lock/journal/root binding, canonical backups, immediate rollback checkpoints, stop-first-error replay, retry skipping, and terminal cleanup; the focused hostile/crash suite passes locally | Authenticated evidence boundary for target-local forgery (H-09), v1 migration decision, and supported-host matrix |
| H-03 | `OPEN_CHANGED` | WP-01A/WP-01B provide protected consumers and Unix mount identity. WP-01C migrates SafeFs reads, writes, copy, initialization, plans, and transactions to checked-in Windows/Linux/macOS handle-relative backends with physical root identity and atomic create/replace. Local Windows PowerShell 5.1 tests cover hard links and synchronized write/read/copy/delete ancestor swaps; the first absolute-name prototype failed that fixture and was discarded. Built-in Git worktree mutation now fails closed while clean external worktrees can be registered read-only. Unix native source compiles and its ABI layouts are statically checked, but this is not native runtime evidence. | Windows PowerShell 7 and native Ubuntu/macOS adversarial evidence; executable capability-schema, contract, drift, and complete supported-host gates |
| H-04 | `OPEN_CHANGED` | WP-04 preserves continuous ownership. WP-05 locally verifies exact-plan-bound `managed-only`, separately confirmed `complete`, and allowlisted hash-verified `export-then-complete`; residual manifests retain tombstoned ownership, fault-injected deletion rolls back and succeeds from a fresh plan, and a real minimal/Codex install returns to an originally empty target. Installer manifests now include every created intermediate and nested skill directory. | Executable receipt-schema validation, independent review, and native PowerShell 7/Ubuntu/macOS lifecycle evidence; direct in-place continuation is intentionally unsupported in favor of reviewed rollback/cleanup and fresh approval |
| H-05 | `OPEN_CHANGED` | WP-06 locally verifies explicit `curated`, `private-episodic`, and physically residue-free `off` contracts across install/update/upgrade/manifest/adapters/doctor/manifest-diff/uninstall. Exact-plan transitions remove only unchanged identity-bound layer artifacts, preserve removed ownership tombstones, block modified/unknown/linked/late content, and roll back injected failure. | Executable schema/mutation gate and native PowerShell 7/Ubuntu/macOS lifecycle evidence; `off` tombstones are intentionally retained as non-executable ownership history rather than forgotten |
| H-06 | `OPEN_CHANGED` | WP-07 makes `DataClass=regulated` an authoritative core-router `human-review` gate before signals, routes, inventories, or recommendations; both model modes, all phases/risks, caller-signal/attempt/model-list bypasses, stable receipt codes, and weakened-policy Doctor detection pass locally. The approval-envelope schema is explicitly structural and not accepted as authority. | Authenticated external issuer and revocation trust from WP-10, signed runtime/provider/model identity, full missing/stale/revoked/mismatched/wrong-region matrix, and supported-host evidence |
| H-07 | `OPEN_CHANGED` | WP-08 removes free-form route signals/evidence prose, adds typed privacy/purpose/audience/retention/content/redaction contracts, and uses a shared serializer. Credential, email, path, command, multiline, Unicode, and oversized canaries are absent from tested routing file/console/error/report/journal-shaped outputs. | Executable schema/mutation evidence, migration of remaining non-routing artifact/report classes, and native PowerShell 7/Ubuntu/macOS evidence |
| H-08 | `OPEN_CHANGED` | WP-09 adds an authority/strict-integrity startup gate to all adapters, installs a prompt-trust protocol, quarantines target-overlay prose while plan/manifest binding exact bytes, and removes free-form verifier commands. Exact approved plans can represent only fixed shell-free Git IDs and bind worktree/executable identities, restrictions, expiry, and output hashes. Hostile overlay drift/prose and command/digest/root/restriction cases pass locally. | Native PowerShell 7/Ubuntu/macOS evidence, external sandbox capability for richer test commands, and host-level proof that IDE/model instruction precedence is actually enforced |
| H-09 | `OPEN_CHANGED` | WP-10 introduces digest-pinned external trust/revocation stores, short-lived challenge nonces, RS256 envelope v2, authenticated implementer/verifier/router/runtime/evaluator roles, decision/plan/runtime/model hashes, external approval references, and replay ledgers. Synthetic PASS, tampering, wrong role/context, revocation, and replay cases pass locally. | PowerShell 7, Ubuntu, and macOS runtime evidence; organization-operated key custody/rotation/revocation exercise; full schema gate after dependencies are available |
| M-01 | `OPEN_CHANGED` | WP-11 uses SafeFs identity-snapshotted traversal, per-child handle/no-follow validation, bounded marker reads, ordinal output, typed evidence strengths, bounded negative signals, qualitative error risk, explicit harness approval, and host-native executable/argv. Local false-positive/negative, link, synchronized-swap, scan-limit, and repeatability cases pass. | Executable target-analysis schema/mutation gate and native PowerShell 7/Ubuntu/macOS matrix; the evidence score is intentionally not statistical calibration |
| M-02 | `OPEN_CHANGED` | WP-12 routes analyzer and install-plan commands through typed host-native executable/argv generation. Synthetic contracts cover Windows PowerShell 5.1, Windows pwsh, Ubuntu pwsh, and macOS pwsh; current-host child execution and no-hard-code assertions pass locally. | Execute emitted preview commands on Windows PowerShell 7, Ubuntu, and macOS and retain their reports |
| M-03 | `OPEN_CHANGED` | WP-13 packages all 21 skills with versions, compatibility, dependencies, permissions, provenance, conflicts, and lifecycle semantics. Local Windows PowerShell 5.1 evidence passes validate/install/idempotent-update/migrate/disable/recover/remove, tamper/dependency rejection, transactional rollback, normal installer mirrors, manifest diff, and strict Doctor. | Executable AJV bindings/mutations after dependencies are available; native Windows PowerShell 7, Ubuntu, and macOS lifecycle runs; independent review of permission declarations |
| M-04 | `OPEN_CHANGED` | WP-14 implements records retention, export, legal-hold, boundary-time verification, and deletion-receipt lifecycle (ADR-0023, `scripts/records-lifecycle.ps1`, `tests/integration/records-lifecycle.tests.ps1`). Local Windows PowerShell 5.1 evidence passes active hold blocks, export integrity, purge, and receipt generation. | Native PowerShell 7/Ubuntu/macOS matrix and organization policy custody review |
| M-05 | `OPEN_CHANGED` | Doctor now deterministically classifies active, recovery-required, cleanup-required, invalid, and orphan transaction metadata | Target-local report residue and cross-command lifecycle consistency tests |
| M-06 | `OPEN_CHANGED` | Every GitHub Actions `npm ci` invocation now uses `--ignore-scripts`, and public-readiness regression assertions fail if any workflow install omits it | A package-level lifecycle-script canary proving suppression on every supported CI host, plus dependency-review and network-capability policy |
| M-07 | `OPEN_CHANGED` | Routing decision and execution receipts now carry consistent sensitivity, purpose, audience, retention, content-policy, and redaction fields | Extend and test the contract across transaction, lifecycle, loop, install/update/uninstall, and diagnostic artifact classes |
| M-08 | `OPEN_CHANGED` | Every adapter now states platform/organization/user precedence, treats repository content as lower trust, and pauses managed-content use until a strict integrity result exists | External IDE/model host-enforcement evidence; repository prose alone cannot enforce a host |
| L-01 | `OPEN_CHANGED` | WP-12 rejects option-like, whitespace/control-bearing, invalid, and revision-expression branch/base inputs before report writes or Git inspection, then resolves commits with `--verify --end-of-options`; the local negative matrix passes. | Supported-host execution of the same negative matrix and review of any future caller-supplied Git ref consumers |

## Supported WP-01A claims

The following narrow claims are supported locally on Windows PowerShell 5.1:

- Ordinary contained files can be read and hashed through the shared safe-filesystem module.
- Existing linked ancestors are rejected before loop evidence metadata or hashes are accepted.
- A linked evidence failure cannot replace an already sealed verifier packet.

These claims do not imply mount-aware containment, race-free mutation, sandboxing, Unix assurance, or full H-03 closure.

## Supported WP-01B claims

The following narrow claims are supported by local Windows PowerShell 5.1 policy and regression evidence plus GitHub Actions run 31319333532 on Ubuntu and macOS:

- Strict synthetic Linux mountinfo records distinguish a nested same-device mount ID from a cross-device mount and reject both with stable `SAFEFS_*` codes.
- Synthetic macOS mounted-root/device records apply the same fail-closed nested-boundary policy.
- SafeFs invokes the mount-boundary guard before returning a resolved target or authorized root, while Windows takes the non-mount path.
- The cleanup-enforcing Ubuntu fixture executed with real bind and `tmpfs` mounts and rejected both boundaries before the longer CI gates.
- The macOS runtime passed the SafeFs and mount-boundary policy suites using the host's live mount enumeration.

These claims do not assert a complete green supported-host matrix: that run exposed a PowerShell 7 JSON timestamp type-conversion incompatibility, while the PowerShell 5.1 job reached its former 35-minute timeout without a test failure. They also do not eliminate a synchronized mount, link, or ancestor swap after validation; that remains WP-01C scope.

Run 31396371758 confirmed that the 120-minute ceiling permits the complete Windows PowerShell 5.1 job to pass in 1 hour 18 minutes. Windows and Ubuntu PowerShell 7 isolated one remaining direct JSON reader in the update-plan binding fixture. macOS additionally exposed its `/var` to `/private/var` host alias in the internal plan-probe temporary path. Neither result closes H-03; a Wave 2 supported-host rerun remains required.

Run 31579015410 executed Wave 2 commit `35437b63cdfb44f2a001511602915454f791f00c`. Both Windows jobs completed successfully. Ubuntu passed the real mount fixture, all 21 focused suites, packs, drift, and quality before the 120-minute ceiling interrupted smoke. macOS passed JSON parsing and multiple install-based suites, demonstrating that the internal probe no longer fails at `/var`, but one SafeFs unit assertion still expected the raw `/var` spelling and the sequential focused run exceeded 120 minutes. These are partial claims only; Wave 3 requires a complete green sharded rerun.
